function cityRecord(name, population, tressury) {
  let city = {
    name: name,
    population: population,
    treasury: tressury,
  };

  return city;
}

cityRecord(
  "Tortuga",

  7000,

  15000
);
